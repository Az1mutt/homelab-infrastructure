schema_version: "0.2"

project:
  name: "Homelab & Media Stack"
  workstream: "Infrastructure, Migration & GitHub"
  owner: "Homelab Infrastructure specialist chat"

status: "active"
phase: "post-migration-stabilization-and-movie-intelligence-foundation"

current_gate: >
  Operationalize the verified Movie Intelligence foundation with a low-frequency
  ČSFD ratings sync, while formal migration closure remains gated on fresh
  application-level acceptance evidence from the media workstream.

current_state: >
  The desktop homelab remains the intended production host, but host/runtime
  details last documented on 2026-09-01 have not been freshly revalidated in
  this workstream and must be treated as stale/unknown where time-sensitive.
  A new local Movie Intelligence foundation is verified on the desktop:
  SQLite media.db is in WAL mode with 682 unique movies, 556 watched/rated,
  126 watchlist, and 0 rows in both states. A one-time private ČSFD watchlist
  bootstrap parsed 142 source items across three pages; 132 movie-like items
  were considered for import and 10 series/season items were excluded for a
  future SerialZone path. Six watchlist items already existed as watched, so
  watched status correctly took precedence. The public ČSFD ratings sync is
  working and a repeat run was idempotent at the data-identity level
  (556 seen, 0 added, 556 existing rows processed). A SQLite-aware pre-import
  backup was created and the bootstrap completed without duplicates.
  GitHub PR #3 for the desktop migration milestone is confirmed merged on
  2026-09-01.

exact_next_action: >
  Confirm the concrete low-frequency ČSFD ratings-sync cadence, then create and
  verify a systemd service/timer for scripts/sync_csfd.mjs; do not schedule it
  daily.

blockers:
  - >
    Formal migration closure is blocked on fresh media-workstream acceptance
    evidence. The latest known requirement set (ARR torrent hardlink behavior
    on ext4 and Kometa revalidation) is from 2026-09-01 and is stale/unknown
    until a new handoff is received.

dependencies:
  - project: "Homelab & Media Stack"
    workstream: "Plex, Kometa & Media Automation"
    dependency: >
      Provide a fresh completion handoff for application-level desktop
      validation, especially ARR torrent hardlinks on ext4 and Kometa status.
      Current dependency status is stale/unknown since 2026-09-01.
  - component: "Movie Intelligence"
    dependency: >
      node-csfd-api and better-sqlite3 are installed and verified; systemd timer
      scheduling remains planned, with cadence intentionally much lower than daily.

important_open_loops:
  - "Choose and deploy the low-frequency ČSFD ratings sync cadence; verify a scheduled run and logging."
  - "Use SQLite-aware backup semantics for future online backups; avoid naive copy while WAL writes may be active."
  - "Keep the one-time ČSFD private watchlist export out of GitHub; it contains private/session-adjacent page data."
  - "Future TV/series ingestion should use a separate SerialZone importer; 10 series/season items were intentionally excluded from the film bootstrap."
  - "Design controlled/idempotent agent operations for add-to-watchlist, mark-watched and future Radarr synchronization instead of unrestricted DB writes."
  - "Retire/unmount the old rollback source disk only after fresh migration-closure evidence; its current attachment state is stale/unknown."
  - "Replace the temporary migration-version override with a permanent container image pin/update policy; current override status has not been freshly revalidated since 2026-09-01."
  - "Continue secrets hardening, backup/restore maturity and later infrastructure monitoring after migration closure."

portfolio:
  milestone: true
  signal: >
    Built a source-agnostic local movie-intelligence data layer on SQLite,
    implemented an idempotent ČSFD ratings ingestion path, merged a private
    watchlist bootstrap with external-ID deduplication and watched-over-watchlist
    precedence, and verified a clean 682-movie state. This adds an auditable
    foundation for future media agents, Radarr integration and multi-source
    enrichment while preserving privacy and rollback safety.

verification:
  last_verified: "2026-09-08"
  sources:
    - "Specialist-chat command output 2026-09-06: media.db sanity check = 682 total / 556 watched / 126 watchlist / 0 both."
    - "Specialist-chat command output 2026-09-06: repeat ČSFD ratings sync = 556 seen / 0 added / 556 existing rows processed."
    - "Specialist-chat import verification 2026-09-06: SQLite-aware pre-import backup created and one-time watchlist bootstrap completed."
    - "Specialist-chat parsed private watchlist snapshot 2026-09-06: 142 source items / 132 movie-like / 10 series-or-season excluded."
    - "GitHub verification 2026-09-08: PR #3 'Document desktop migration milestone' is merged (merged 2026-09-01)."
    - "docs/current-state.md and docs/roadmap.md snapshot dated 2026-09-01; time-sensitive migration details from these files are stale/unknown unless separately refreshed."

sync:
  status: "pending"
  storage: ".project/state.yaml"
